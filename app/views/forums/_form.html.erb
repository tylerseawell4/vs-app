<%= form_for(@forum) do |f| %>
  <% if @forum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@forum.errors.count, "error") %> prohibited this forum from being saved:</h2>

      <ul>
      <% @forum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-grop">
    <%= f.text_field :title, class: "form-control", placeholder: "Put Title" %>
  </div>
  <div class="form-group">
  <%= f.text_area :entry, class: "form-control", :rows => 20, placeholder: "Write it Down" %>
  </div>
   <div class="form-group cant-see-me">
    <%= f.number_field :user_id, value: current_user.id %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-danger" %>
    </div>
  
<% end %>